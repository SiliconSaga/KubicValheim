VALHEIM_NAME=KubicValheim
# REQUIRED: set your own before `docker compose up` (>= 5 chars, must not contain
# the server NAME). Left empty so a copied .env can't start with a known password —
# Compose fails fast until you fill it in.
VALHEIM_PASSWORD=
VALHEIM_WORLD=Dedicated
